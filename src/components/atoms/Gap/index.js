import React from 'react'
import {Text,View,StyleSheet} from 'react-native'

const Gap = (props)=>{
    return(
        <View style={{
            width: props.width,
            height: props.height
        }}>
            
        </View>
    )
}

export default Gap